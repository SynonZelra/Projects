#include "HID-Project.h"

bool stat = 0;

void setup()
{
  pinMode(9,INPUT_PULLUP);
  Keyboard.begin();
}

void loop()
{
  if ( stat==0 && digitalRead(9)==0)
  {
    Keyboard.press('a');
    stat=!stat;
  }
  if ( stat==1 && digitalRead(9)==1)
  {
    Keyboard.release('a');
    stat=!stat;
  }
}
